{erl_opts, [no_debug_info]}.
{deps, [argparse]}.

{escript_incl_apps,
 [argparse, conf_reader]}.
{escript_main_app, conf_reader}.
{escript_name, conf_reader}.
{escript_emu_args, "%%! +sbtu +A1\n"}.

%% Profiles
{profiles, [{test,
             [{erl_opts, [debug_info]}
            ]}]}.
